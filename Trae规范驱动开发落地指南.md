# Trae 规范驱动开发（SDD）落地指南

> 目标：用文档替代对话历史作为 AI 的长期记忆，让每轮问答的上下文成本从 O(N) 降到 O(1)，同时让 AI 对"项目现在长什么样"有确定性的认知。
>
> 适用：Trae IDE / Trae SOLO（v3.5.41+，支持 Skills）
>
> 版本：v1.0 · 2026-08

---

## 0. 先纠正一个关键认知

原始想法是"每次问答生成一个功能文档"。方向对，但**粒度错了**。

| | 每次问答生成文档 | 正确做法 |
|---|---|---|
| 文档数量 | 100 次问答 = 100 份 | 按功能模块，一个模块一份 |
| 版本冲突 | 第 37 份说用 JWT，第 62 份说改成 Session，AI 不知道信谁 | 单一真相源，只有 current 和 archive |
| 额外开销 | 写文档比写代码还贵 | 只在"探索→实现"的转折点写一次 |
| 信息密度 | 20 个碎片小文档 | 1 份结构化模块说明 |

**触发时机不是"每次问答"，而是"一个功能模块从探索进入实现"的那一刻。**

这个纠正很重要，因为它决定了后面所有流程的设计。

---

## 1. 为什么必须这么做：三个真实存在的失败模式

不这么做，AI 编程会在项目超过一定规模后稳定地崩在三个地方：

**意图漂移** — "添加登录"这种提示严重不完整。模型会选一个合理的默认行为，但 rarely 是你想要的那个。

**上下文衰减** — 代码库超过有效上下文窗口后，AI 忘记早期决策，并默默与之矛盾。对话到第 50 轮时，早期废弃方案的试错过程仍占着窗口。

**输出不可验证** — 没有明确的验收标准，就无法判断生成的代码"对不对"，Code Review 变成无底洞。

而 token 的账也算得清：AI 编码工具每发一条新消息都会带上完整对话历史。30 条消息的对话里，第 20-30 条每条都背着 40k-60k 的历史，**最后 10 条的成本超过前 20 条的总和**。反过来，一份 500 token 的项目指令文件加载一次，能省掉 20+ 次提示里重复的背景交代，净省 15-25% 输入 token。

---

## 2. Trae 的四层上下文机制（这是落地的基础）

Trae 有一套其他工具不完全一样的机制，理解它才能把该放的东西放对位置。

### 2.1 四层总览

| 层级 | 位置 | 加载时机 | Token 成本 | 放什么 |
|---|---|---|---|---|
| **L0 项目总纲** | `AGENTS.md`（项目根目录） | 每轮对话都注入 | 每轮都付，必须极简 | 项目定位 + 文档索引 + 铁律 |
| **L1 项目规则** | `.trae/rules/*.md` | 按生效方式 | 始终生效 = 每轮付 | 代码规范、测试约定、安全红线 |
| **L2 技能** | `.trae/skills/*/SKILL.md` | **按需，显式调用才加载** | **只有用到才付** | 多步骤流程：生成功能文档、回写文档 |
| **L3 记忆** | `~/.trae-cn/memory/` 项目记忆 | 自动累积 | 低 | 协作中沉淀的偏好 |

**L2 是这一整套方案的核心杠杆。** 规则是常驻的、每个 session 都付钱；Skills 是按需的、只有用到才消耗。把"生成功能文档""回写功能文档"这类多步骤流程做成 Skill，平时不占上下文，需要时一个 `/spec-new` 调出来。

### 2.2 四种规则生效方式

`.trae/rules/` 下的规则通过 YAML frontmatter 控制触发：

```yaml
---
description: 适用场景描述（智能生效时 AI 据此判断）
alwaysApply: false
globs:
  - "src/api/**"
---
```

| 生效方式 | 配置 | 用途 |
|---|---|---|
| 始终生效 | `alwaysApply: true` | 代码风格、安全红线 |
| 指定文件生效 | `globs: [...]` | 前端模块规则、API 设计规范 |
| 智能生效 | 写好 `description`，由 AI 判断 | 场景性强的规则 |
| 手动触发生效 | 对话中用 `#Rule` 提及 | 低频但需要严格执行的流程 |

**容易被忽略的技巧**：`.trae/rules/` 支持放在子目录，系统递归读取（最多三层）。大型项目不要把规则堆在根目录，前端模块放 React 规范、后端模块放 API 规范，互不干扰。

### 2.3 优先级与两个开关

优先级：**用户输入 > 智能体提示词 > 个人规则 > 项目规则**。

两个必须手动打开的开关：

```
设置 > 规则 > 导入设置
  ☑ 将 AGENTS.md 包含在上下文中
  ☑ 将 CLAUDE.md 包含在上下文中
```

不开这两个开关，根目录的 `AGENTS.md` 不会被读取。

> ⚠️ 注意：在 TRAE SOLO 中，开启后 `AGENTS.md` 会在**每一轮对话**都加入上下文。这正是为什么 L0 必须极简——它每轮都计费。
>
> ⚠️ 另一点：`AGENTS.md` 修改后当前会话不会自动感知，需要开新任务才生效。

---

## 3. 工具选型：要不要上 OpenSpec / Spec Kit

社区在 2026 年已经收敛出明确的选型共识，取决于你的项目状态。

### 3.1 决策表

| 维度 | Spec Kit | OpenSpec | 什么都不装 |
|---|---|---|---|
| 出品方 | GitHub | Fission AI | — |
| 技术栈 | Python / uv | TypeScript / npm | — |
| 设计哲学 | 项目宪法 + 全流程门控 | 轻量变更管理 + 增量迭代 | 手写文档 + Skills |
| 核心流程 | `constitution → specify → plan → tasks → implement` | `propose → apply → archive` | 自定义 |
| **适合** | **0→1 新项目、需求模糊、高合规** | **1→N 存量项目、小团队日常迭代** | 探索原型、个人小项目 |
| 学习成本 | 较高 | 10 分钟上手 | 无 |
| 文档产出 | 重，单阶段可达 800+ 行 | 轻，约 250 行 | 自己控制 |
| 变更追踪 | 无内建机制，靠 Git | **Delta Spec + 归档历史** | 靠 Git |
| Token 消耗 | 高 | 较低 | 最低 |
| 存量项目支持 | 差，需先补大量文档 | **好，改哪补哪** | 好 |

### 3.2 推荐策略：组合使用

你的情况是新项目和老项目都有，**社区共识的做法是两个都装，按场景切换**：

- **新项目启动阶段** → 用 Spec Kit 立宪法和初始架构，产出项目级约束
- **项目进入稳定迭代** → 切到 OpenSpec，用 delta 管理日常增量
- **两者都不想装的时候** → 至少要有一份 `AGENTS.md` + 两个自定义 Skill（见第 5 节），这已经能拿到 80% 的收益

OpenSpec 的 delta 机制是它为存量项目设计的底气：**你不改的代码不需要写文档**。第一次变更只文档化它触碰的那一片，下一次变更再补下一片，几个月后 spec 自然长成围绕真实工作的形状。

### 3.3 但是，先看清社区的批评

在装之前，这些是真实用户踩出来的坑：

- **Markdown 膨胀**：一次 plan 阶段生成 2000+ 行 Markdown，包含一份 406 行的重复研究文档。有测试报告显示生成约 700 行代码配了近 2500 行文档，开发者花 3.5 小时审查这些文档。
- **Token 燃烧**：每次交互都要注入 constitution.md、spec.md、plan.md，有用户抱怨周四就烧完了月度额度。
- **改个变量名也要改规范**：严格 SDD 要求回溯修改 Spec → 重新生成 Plan → 更新 Task → 再改代码。热修复时极其反人类。
- **存量项目支持差**：复杂遗留项目上，AI 无法凭上下文准确归纳出"合理通用的 spec"，生成的 Plan 和 Task 往往基于错误假设。
- **跳过一次 Spec，之前所有规范直接过时**：没有容错率，没有反向同步。

**所以下面这套方案刻意做了削减**——只保留收益最大的部分，砍掉仪式性的部分。

---

## 4. 目录结构

把配置和文档都放进仓库，跟代码一起版本化：

```
your-project/
├── AGENTS.md                    # L0 项目总纲（极简，只放索引和铁律）
├── .trae/
│   ├── rules/
│   │   ├── project_rules.md     # 代码规范（alwaysApply: true）
│   │   ├── testing.md           # 测试约定（智能生效）
│   │   ├── api-conventions.md   # API 规范（globs 生效）
│   │   ├── frontend/
│   │   │   └── react.md         # 子目录规则，递归读取
│   │   └── backend/
│   │       └── api.md
│   ├── skills/
│   │   ├── spec-new/SKILL.md    # 新功能前生成功能文档
│   │   ├── spec-sync/SKILL.md   # 功能完成后回写文档
│   │   └── adr/SKILL.md         # 记录架构决策
│   └── commands/                # 可选：自定义斜杠命令
└── docs/
    ├── ARCHITECTURE.md          # L1 架构地图（整个项目一份）
    ├── PRODUCT.md               # 产品功能总览
    ├── features/                # L2 功能规格（一个模块一份）
    │   ├── order.md
    │   ├── payment.md
    │   └── user.md
    └── adr/                     # L3 决策记录
        ├── 001-轮询替代WebSocket.md
        └── 002-订单状态机设计.md
```

如果装了 OpenSpec，额外会有 `openspec/specs/`（主规范库）和 `openspec/changes/`（变更提案）。

---

## 5. 三个核心文件模板

### 5.1 AGENTS.md（L0 项目总纲）

**原则：只放指针，不放细节。** 它每轮对话都被注入，每多一行都在每轮计费。

```markdown
# 项目：XXX 电商后台

一句话定位：面向中小商家的订单与库存管理系统。
技术栈：Next.js 15 · Prisma · PostgreSQL 15 · Tailwind · Node 20

## 常用命令
- 安装依赖：`pnpm install`
- 开发服务：`pnpm dev`
- 测试：`pnpm test`（提交前必须通过）
- 类型检查：`pnpm typecheck`
- Lint：`pnpm lint`

## 文档索引（按需读取，不要一次性全读）
| 我要做什么 | 读这个 |
|---|---|
| 理解整体架构 | `docs/ARCHITECTURE.md` |
| 改订单相关 | `docs/features/order.md` |
| 改支付相关 | `docs/features/payment.md` |
| 改用户相关 | `docs/features/user.md` |
| 想知道为什么这么设计 | `docs/adr/` 下的决策记录 |

## 铁律
1. **修改任何模块前，先读该模块的 feature 文档**。文档索引在上表。
2. **功能完成后，用 `/spec-sync` 同步更新对应文档**。文档没更新 = 功能没做完。
3. 不确定的地方先问，不要猜。猜错的成本远高于问一次。
4. 文档写"意图和约束"，不写"实现细节"——代码是唯一真相源。

## 边界
- Always：改完跑 `pnpm test` 和 `pnpm typecheck`
- Ask First：修改数据库 schema、改动公共 API 签名、新增依赖
- Never：直接改 `docs/adr/` 下已有决策（要改就新建一条 ADR 说明废弃原因）
```

**反面教材**（社区研究证实有效的避坑点）：

研究者在 138 个真实仓库上做过测试，**LLM 自动生成的 context file 会降低 agent 任务成功率，同时让推理成本上升 20% 以上**；开发者手写的只有 +4% 的提升——**而且只在"极简且精确"时才成立**。不必要的约束会主动伤害性能，不是因为 AI 忽略了它们，而是因为 AI 会忠实地执行它们，从而扩大探索空间、增加推理成本却没提升结果。

所以：能用工具链强制的，就不写进 AGENTS.md。

```markdown
# ❌ 错误：把 linter 能管的事写进 AGENTS.md
- 不用 var
- import 顺序按字母排
- 缩进两空格

# ✅ 正确：只留指针
- Lint：`pnpm lint`（Biome，规则见 `biome.json`）
```

另一条：**小心"粉红大象效应"**。告诉 LLM "不要用 tRPC"，反而让 tRPC 这个词在注意力机制里高度活跃，它可能还是会去够。每条"不要做 X"的指令，都是一处代码库摩擦的信号——**正确的做法是从结构上消除它**（真的删掉那个 legacy 工具、加条 lint 规则），然后把这条指令从 context file 里删掉。

### 5.2 功能规格文档（L2）

这是你想要的那个"功能文档"。**触发时机：功能从探索进入实现前。**

```markdown
---
module: order
status: in_progress        # planning | in_progress | done | deprecated
updated: 2026-08-31
owner: @张三
---

# 订单模块

## 要解决什么问题
用户下单后 30 分钟未支付自动取消并释放库存，避免恶意占单。

## 需求
- [ ] 创建订单时预扣库存，写入 expireAt = now + 30min
- [ ] 定时任务扫描过期未支付订单，取消并回滚库存
- [ ] 支付回调晚于自动取消时，走退款流程而非恢复订单

## 接口
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/orders` | 创建订单，预扣库存 |
| POST | `/api/orders/:id/cancel` | 取消订单，回滚库存 |

## 数据模型
```
Order: id, userId, status(pending|paid|cancelled), expireAt, items[], version
```
`version` 用于乐观锁控制并发。

## 边界 Case（重点关注）
- **并发超卖**：DB 行锁 + 乐观锁版本号双重保障
- **支付回调迟到**：已取消订单收到支付成功回调 → 触发退款，不恢复订单
- **重复取消**：幂等，重复调用不重复回滚库存

## 依赖与被调用
- 依赖：payment 模块的退款接口
- 被调用：admin 后台订单列表、user 中心的我的订单

## 验收标准
- [ ] 订单创建后 30 分钟自动取消（定时任务验证）
- [ ] 并发 100 单不出现超卖
- [ ] 迟到的支付回调走退款流程
- [ ] 重复取消接口幂等

## 不做的事（Out of Scope）
- 不支持部分退款（v2 再议）
- 不支持修改已提交订单的商品

## 相关决策
- 为什么用轮询不用 WebSocket：见 `docs/adr/001-轮询替代WebSocket.md`
```

`status` 和 `updated` 两个字段是关键——它们让 AI 能判断这份文档**可不可信**。

### 5.3 ADR 决策记录（L3）

最容易被忽略、但价值极高的一层。**没有它，AI 每隔几轮就会把一个你早就否决的方案重新提议一遍。**

```markdown
# ADR-001：订单状态推送用轮询而非 WebSocket

- 日期：2026-08-20
- 状态：已接受
- 决策人：@张三

## 背景
订单状态需要实时展示给用户，团队讨论过 WebSocket / SSE / 轮询三种方案。

## 结论
采用轮询，5 秒间隔。

## 理由
- 订单状态变更频率极低（日均 1000 单，峰值 QPS < 1）
- WebSocket 需要独立的连接管理服务，运维成本不匹配业务量级

## 否决的方案
- **WebSocket**：性能好但运维成本高，杀鸡用牛刀
- **SSE**：公司网关对长连接兼容性有问题，联调踩过坑

## 何时重新评估
日订单量超过 5 万，或用户反馈状态更新不及时时。
```

---

## 6. 把流程固化成 Skill

这是整套方案从"我记住要做"变成"工具会提醒我做"的关键。

### 6.1 `/spec-new` — 新功能开工前

创建 `.trae/skills/spec-new/SKILL.md`：

```markdown
---
name: spec-new
description: 开始一个新功能模块前，生成结构化的功能规格文档。当用户提出要实现一个新功能、且这个功能会触及多个文件或需要跨会话完成时，使用此技能。
---

# 生成功能规格文档

## 前置检查
1. 读取 `AGENTS.md` 的文档索引，确认是否已有相关模块文档
2. 如果 `docs/features/` 下已有同名模块，先读它，做增量更新而非重建
3. 如果项目用了 OpenSpec，改用 `/opsx:propose` 流程

## 生成步骤
按以下顺序与我确认，不要一次性全问：
1. **要解决什么问题** — 一句话。如果我说不清楚，帮我问到清楚为止
2. **需求清单** — 拆成可勾选的条目
3. **接口设计** — 方法、路径、作用
4. **数据模型** — 字段和状态机
5. **边界 Case** — 这里是重点，帮我穷举：并发、超时、重复调用、数据不一致
6. **依赖与被调用** — 上下游模块
7. **验收标准** — 可勾选的测试点
8. **Out of Scope** — 明确不做什么

## 输出
写入 `docs/features/<module>.md`，frontmatter 包含：
```
module, status: planning, updated: <今天>, owner
```

## 注意
- 文档写"意图和约束"，不写"实现细节"——函数签名会变，抄进来两周就误导人
- 边界 Case 这一节宁可多花时间，这是文档最大的价值所在
- 生成后让我 review，**我确认后才开始写代码**
```

### 6.2 `/spec-sync` — 功能完成后回写

创建 `.trae/skills/spec-sync/SKILL.md`：

```markdown
---
name: spec-sync
description: 功能实现完成后，回写更新对应的功能规格文档。当用户说功能做完了、要提交了、或要开新会话时，使用此技能。
---

# 回写功能文档

## 步骤
1. 找到本次改动触及的模块，读 `docs/features/<module>.md`
2. 用 `git diff` 看实际改了什么
3. 逐节比对，更新以下内容：
   - frontmatter 的 `status` 和 `updated`
   - 需求清单里已完成的条目打勾
   - 接口设计：实际签名与文档不符的，**以代码为准更新文档**
   - 验收标准：已验证的打勾
   - 新增的边界 Case（实现过程中才发现的）
4. **压缩**过程性内容：调试中的试错、被否决的中间方案，不要留在文档里
5. 如果本次实现推翻了某个已有决策，提醒我新建一条 ADR，不要直接改旧 ADR

## 铁律
- 文档是给"下一轮会话的 AI"读的，不是给现在的我读的
- 保持精简。一份功能文档超过 200 行说明该拆分模块了
- 如果实现方式与设计文档有偏差，先问我是"改文档"还是"改代码"
```

### 6.3 `/adr` — 记录架构决策

创建 `.trae/skills/adr/SKILL.md`：

```markdown
---
name: adr
description: 记录一个重要的架构决策或技术选型。当我们讨论过多个方案、或做出了一个未来可能被质疑的选择时，使用此技能。
---

# 记录架构决策

在 `docs/adr/` 下新建 `<序号>-<决策主题>.md`，序号递增。

## 必填内容
- 背景：为什么需要做这个决策
- 结论：选了什么
- 理由：为什么（数据量级、团队能力、运维成本等具体数字）
- **否决的方案 + 否决原因**（这一节最重要，防止 AI 以后重新提议）
- 何时重新评估：给出触发条件

## 什么样的决策值得记
- 有多个可选方案，且选错了成本很高
- 未来大概率会被人（或 AI）质疑"为什么不那样做"
- 涉及权衡取舍（性能 vs 成本、一致性 vs 可用性）

## 不值得记的
- 只有唯一合理选项的
- 纯实现细节（用 for 还是 forEach）
```

### 6.4 为什么用 Skill 而不是 Rule

这是 Trae 机制里最省 token 的一个点：

| | Rules | Skills |
|---|---|---|
| 加载 | 常驻，每个 session 都加载 | **按需，只有显式调用才加载** |
| Token 成本 | 每个 session 都付 | 只有用到才付 |
| 调用 | 自动（按模式） | 显式调用 |

把上面三个流程做成 Skill，平时完全不占上下文；需要时 `/spec-new` 一句话调出来。**这是"用到才付费"，而不是"包月"**。

---

## 7. 团队规范：Right-sized SDD

**最重要的一条：不要把完整流程套在所有任务上。**

社区的惨痛教训：用 SDD 修一个简单 Bug，被要求生成 4 个用户故事和 16 个验收标准——**用大锤砸坚果**。

### 7.1 按任务规模分档

| 任务规模 | 判断标准 | 该做什么 |
|---|---|---|
| **微小** | 改个变量名、修个明显 bug、单文件小改 | **什么都不用做**，直接聊 |
| **小** | 1-2 个文件，半天内能完成 | 口头对齐，不写文档。写完用 `/spec-sync` 顺手更新 |
| **中** | 跨 2+ 模块，1-3 天 | 完整走 `/spec-new` → review → 实现 → `/spec-sync` |
| **大** | 跨服务、需要架构决策 | 完整流程 + ADR + OpenSpec/Spec Kit |

**经验阈值**：触及超过两个组件、或耗时超过一天的，才走完整流程。其他一律直接 prompt。

### 7.2 什么是"完成"

写进团队 Definition of Done：

```
[ ] 代码通过 pnpm test 和 pnpm typecheck
[ ] 触及的模块文档已用 /spec-sync 更新
[ ] 新增的重要决策已记录 ADR
[ ] 文档里的验收标准全部打勾
```

**第二条是关键**。功能没更新文档 = 没做完。否则文档必然过期——而**过期的文档比没有文档更糟**，因为 AI 会自信地生成满足过期契约的代码，人类评审者会以为契约是最新的。

### 7.3 会话节奏

配合这套文档，会话应该是这样切的：

```
探索/调试    → 随便聊，不写文档
方案定了     → /spec-new 生成文档，人工确认
实现中       → 正常迭代
功能完成     → /spec-sync 更新文档，status 改 done，压缩过程性内容
上下文快满   → 直接开新会话，靠文档续上
话题切换     → 直接开新会话
```

最后两条是 token 节省的大头：**有了文档，你就可以毫无心理负担地丢弃对话历史**，而不是花力气去总结压缩它。

操作建议：**每 20-30 条消息开一次新会话**。40k-60k 的历史在第 20 轮之后开始急剧烧钱，而带一份好摘要的新上下文，产出的质量反而比臃肿的历史更好。

---

## 8. 两周落地路线图

### 第一周：个人先跑通

| 天 | 做什么 | 产出 |
|---|---|---|
| 1 | 在项目根目录写 `AGENTS.md`，只写：定位、技术栈、命令、索引、铁律 | 一份 < 60 行的文件 |
| 1 | 打开 设置 > 规则 > 导入设置 的 AGENTS.md 开关 | — |
| 2 | 让 AI 读一遍代码库，生成 `docs/ARCHITECTURE.md`（限 2 页内） | 架构地图 |
| 2 | **人工审校**这份架构文档，删掉所有废话和推测 | 可信任的 L1 |
| 3 | 创建三个 Skill：`spec-new` / `spec-sync` / `adr` | 流程固化 |
| 4-5 | 挑一个**正在做**的中等功能，完整走一遍 `spec-new` → 实现 → `spec-sync` | 第一份 L2 文档 |

**关键：不要为文档而文档。** 挑一个你这周本来就要做的功能来跑流程，让文档从真实工作中长出来，而不是额外补出来的。

### 第二周：沉淀规则 + 团队推广

| 天 | 做什么 |
|---|---|
| 6-7 | 把代码规范、测试约定整理成 `.trae/rules/` 下的规则文件，按 globs 或智能生效配置 |
| 8 | 用 `/adr` 补 3-5 条最重要的历史决策**（尤其是那些"为什么不用 X"的）** |
| 9 | 在团队内做一次 30 分钟分享，讲清：为什么要做、三个 Skill 怎么用、什么规模的任务该跳过 |
| 10 | 把"文档已更新"加进 PR 模板的 checklist |
| 持续 | 每月检查一次文档漂移：哪些文档 `updated` 超过 90 天没动过 |

### 可选：第三周引入 OpenSpec

如果团队跑通了上面的流程，且主要在存量项目上迭代，可以引入 OpenSpec 做更强的变更追踪：

```bash
npm install -g @fission-ai/openspec@latest
cd your-project
openspec init --tools claude    # Trae 选其他兼容项或手动配置
```

核心三步：`/opsx:propose` → `/opsx:apply` → `/opsx:archive`。归档时 delta spec 自动合并进主规范库。

**但注意**：OpenSpec 的 delta 模型假设 AI 理解代码库中未变更的部分。如果项目内联文档很差，AI 可能幻觉出周围的上下文。这时候先用 `/opsx:explore` 让它读一遍相关区域。

---

## 9. 防坑清单

按踩坑频率排序：

**① 文档膨胀**
单份功能文档超过 200 行就该拆模块了。如果审查文档的负担超过审查代码，工程师会开始敷衍，"共享真相源"会迅速腐化。

**② 文档漂移**
这是最大的风险。缓解手段：
- `updated` 字段让 AI 能判断可信度
- PR checklist 强制更新
- 定期跑一次漂移检查（`git log` 找 90 天没动的文档）

**③ AGENTS.md 里堆细节**
它每轮对话都注入。研究结论很明确：**不必要的约束会主动降低 agent 表现**。能用 lint/type check/CI 强制的，一条都不要写进去。

**④ 对小任务套完整流程**
修个 bug 生成 4 个用户故事 + 16 个验收标准，团队会立刻开始抵制整套方案。

**⑤ 粉红大象效应**
"不要做 X" 反而强化了 X。从结构上消除问题（删掉 legacy 代码、加 lint 规则），然后从文档里删掉这条指令。

**⑥ 改了 AGENTS.md 但没开新会话**
Trae 里修改后当前会话不会自动感知，需要开新任务才生效。这也是官方文档的建议：新建或修改规则后，建议开启全新对话。

**⑦ 让 AI 自己写测试并自己判卷**
Agents 通过自己生成的测试有着"令人沮丧的可靠性"。**验收标准应该写在 spec 里，而不是让 agent 自己发明**。

---

## 10. 一页速查

```
┌─ 开工前 ────────────────────────────┐
│ 微小任务 → 直接聊，什么都不写        │
│ 中等任务 → /spec-new 生成文档 → 人工确认 │
│ 大型任务 → + ADR + OpenSpec/Spec Kit  │
└────────────────────────────────────┘
              ↓
┌─ 实现中 ────────────────────────────┐
│ 每 20-30 条消息开新会话              │
│ 靠文档续上，不靠压缩历史              │
└────────────────────────────────────┘
              ↓
┌─ 完成时 ────────────────────────────┐
│ /spec-sync 回写文档                 │
│ 重大决策 → /adr                     │
│ status: done + 更新 updated          │
│ 文档没更新 = 没做完                  │
└────────────────────────────────────┘
```

**四层文档的职责边界**

| 层 | 文件 | 回答什么问题 | 数量 |
|---|---|---|---|
| L0 | `AGENTS.md` | 怎么在这个项目里干活 | 1 份（极简） |
| L1 | `ARCHITECTURE.md` | 系统怎么组织的 | 1 份 |
| L2 | `features/*.md` | 这个模块要干什么、边界在哪 | 每模块 1 份 |
| L3 | `adr/*.md` | 为什么这么选、否决了什么 | 每个决策 1 份 |

---

## 附录：关键配置速查

| 配置 | 位置 | 说明 |
|---|---|---|
| 项目总纲 | 根目录 `AGENTS.md` | 需在 设置 > 规则 > 导入设置 开启 |
| 个人规则 | `~/.trae-cn/user_rules` | IDE 管理，不要手动放置 |
| 项目规则 | `.trae/rules/*.md` | 支持子目录递归（最多三层） |
| 技能 | `.trae/skills/{name}/SKILL.md` | v3.5.41+，按需加载 |
| 开放标准技能 | `.agents/skills/` | 跨 agent 互操作 |
| 斜杠命令 | `.trae/commands/` | v3.5.54+，最多三层嵌套 |
| MCP 配置 | `.trae/mcp.json` | — |
| 全局记忆 | `~/.trae-cn/memory/user_profile.md` | 跨项目 |
| 项目记忆 | 按项目路径独立存储 | 设置 > 记忆 开启 |

**规则优先级**：用户输入 > 智能体提示词 > 个人规则 > 项目规则
